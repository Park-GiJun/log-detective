# Code Review — b48306a

> feat: log-ingest-service 헥사고날 아키텍처 전체 구현
> 2026-04-19 | 21 files, +575

## 기술 심각도: HIGH | 품질 심각도: HIGH

---

## Reviewer 1 — 강현수 (Architect) | HIGH | 4건

### 잘한 점
- DTO 분리, 포트 접미어 네이밍, HandlerConfig @Bean 등록 규칙 일치
- Response DTO가 infrastructure 계층에 분리되어 도메인 노출 방지

### 개선 필요
| # | 파일 | 심각도 | 설명 |
|---|------|--------|------|
| 1 | LogEventQueryHandler.kt | CRITICAL | `import com.gijun.fds.common.exception` — 구 패키지(fds) 직접 참조 |
| 2 | LogEventCommandHandler.kt | HIGH | `open class` 선언 — Spring AOP 목적이라면 설계 의도 누락 |
| 3 | LogEventPersistencePort.kt | MEDIUM | `Pair<LogEvent, Instant>` — 의미 불명확, 전용 data class 권장 |
| 4 | LogEventCommandHandler.kt | MEDIUM | `UUID.randomUUID()` 직접 호출 — ID 생성 전략 하드코딩 |

---

## Reviewer 2 — 박서진 (Security) | CRITICAL | 5건

### 잘한 점
- CSRF 비활성화는 REST API 특성상 적절
- LogLevel enum 검증으로 허용 값 제한 시도

### 취약점
| # | 파일 | 심각도 | 설명 |
|---|------|--------|------|
| 1 | SecurityConfig.kt | CRITICAL | `anyRequest().permitAll()` — 인증/인가 전면 부재. OWASP A01 위반 |
| 2 | LogEventWebAdapter.kt | HIGH | `limit` 파라미터 상한 없음 → DoS 유발 가능 |
| 3 | LogEventWebAdapter.kt | HIGH | 입력 검증 전무 — message 필드 길이 제한 없음 |
| 4 | LogEventEntity.kt + Kafka + ES | HIGH | IP, userId 평문 저장·발행·인덱싱 |
| 5 | IngestEventCommand.kt | MEDIUM | `LogLevel.valueOf()` 예외가 500으로 노출 |

---

## Reviewer 3 — 이도윤 (Performance) | HIGH | 5건

### 잘한 점
- ES BulkRequest 사용, JPA batch insert, Kafka key를 eventId로 고정

### 성능 이슈
| # | 파일 | 심각도 | 설명 |
|---|------|--------|------|
| 1 | LogEventCommandHandler.kt | CRITICAL | `@Transactional` 안에서 ES+Kafka 동기 실행 → p99 폭발 |
| 2 | LogEventMessageAdapter.kt | HIGH | `publishRawBatch()` 건건 send → 배치 효율 없음 |
| 3 | LogEventPersistenceAdapter.kt | HIGH | `findTop100..take(limit)` — 항상 100건 fetch |
| 4 | LogEventMessageAdapter.kt | HIGH | Kafka Future 무시 → 발행 실패 시 데이터 유실 |
| 5 | LogEventSearchAdapter.kt | LOW | `toDocument()` 매번 Map 생성 |

---

## Reviewer 4 — 정하은 (Code Quality) | MEDIUM | 5건

### 잘한 점
- Handler 접미어 정확 사용, trailing comma 일관 적용, toLogEvent() 분리

### 개선 필요
| # | 파일 | 심각도 | 설명 |
|---|------|--------|------|
| 1 | LogEventQueryHandler.kt | HIGH | `fds` 패키지 혼재 |
| 2 | LogEventPersistencePort.kt | MEDIUM | `Pair` 반환 타입 → 전용 data class 권장 |
| 3 | LogEventSearchAdapter.kt | MEDIUM | `Map<String, Any?>` 타입 안전성 부족 |
| 4 | LogEventResponse.kt | LOW | LogEventResult과 필드 동일 — 계층 분리 의도 주석 권장 |
| 5 | IngestEventRequest.kt | LOW | `timestamp ?: Instant.now()` — 테스트 재현성 저하 |

---

## Reviewer 5 — 김태현 (Testing) | HIGH | 7건

### 잘한 점
- 의존성이 포트 인터페이스로만 구성되어 MockK 치환 용이

### 테스트 누락
| # | 대상 | 심각도 | 설명 |
|---|------|--------|------|
| 1 | LogEventCommandHandler | CRITICAL | LogLevel.valueOf 예외 미처리 + 테스트 없음 |
| 2 | LogEventCommandHandler | HIGH | DB저장→ES실패 시 롤백 검증 없음 |
| 3 | LogEventCommandHandler | HIGH | 빈 배치, 부분 실패 시나리오 없음 |
| 4 | LogEventQueryHandler | HIGH | getByEventId 존재/미존재 테스트 없음 |
| 5 | LogEventWebAdapter | MEDIUM | REST 엔드포인트 4개 테스트 없음 |
| 6 | LogEventEntity | MEDIUM | from()/toDomain() 변환 왕복 검증 없음 |
| 7 | LogEventPersistenceAdapter | MEDIUM | JPA 실제 쿼리 검증 없음 |

---

## Reviewer 6 — 윤지아 (Domain Expert) | HIGH | 4건

### 잘한 점
- event_timestamp vs ingested_at 분리 — 수집 지연 분석 가능
- ES 날짜 기반 인덱스 분리 구조

### 도메인 이슈
| # | 파일 | 심각도 | 설명 |
|---|------|--------|------|
| 1 | LogEventCommandHandler | HIGH | DB↔ES/Kafka 불일치 → 탐지 누락(미탐) 발생 가능 |
| 2 | toLogEvent() | MEDIUM | 클라이언트 timestamp 무조건 신뢰 → 오탐/미탐 유발 |
| 3 | LogEventSearchAdapter | MEDIUM | UTC 기준 ES 인덱스 → KST 경계 왜곡 |
| 4 | batch-max-size | LOW | 설정만 있고 코드 제한 없음 |

---

## Tech Lead 종합 — 최민준

### Action Items (즉시 수정)
1. `anyRequest().permitAll()` → 최소 인증 필터 적용
2. `@Transactional` 경계에서 ES/Kafka 호출 분리 — after-commit hook
3. Kafka `send()` Future 예외 처리
4. `fds` 패키지 import 제거

### Tech Debt
| 등급 | 내용 |
|------|------|
| HIGH | limit 상한 없음 → DoS 노출 |
| HIGH | 입력 검증 전무 |
| MEDIUM | findRecent 하드코딩 100건 |
| MEDIUM | publishRawBatch 건건 send → 배치 처리 |
| LOW | Pair 타입 → 명시적 data class |

### 반복 실수 패턴 업데이트
| # | 패턴 | 상태 |
|---|------|------|
| 1 | 도메인 타입 불일치 (Long/Int/Double) | 지속 |
| 2 | jakarta vs springframework 혼동 | 유지 |
| 3 | @Transactional 경계 내 외부 I/O 혼입 | **신규** |
| 4 | 비동기 Future/응답 무시 (fire-and-forget) | **신규** |

---

## Quality Lead 종합 — 한소율

### 학습 포인트
1. **"저장 성공 ≠ 처리 성공"** — DB 커밋 후 ES/Kafka 실패는 부분 성공. Outbox 패턴으로 원자성 보장.
2. **"도메인 경계에서 방어하라"** — 외부 입력은 Command 계층에서 검증·변환. 내부 도메인은 유효한 값만.
3. **"타입은 의미를 담는다"** — Pair 대신 명명된 data class로 의도 명시.

### Tech Debt 추가
| ID | 내용 | 우선순위 |
|----|------|----------|
| QD-01 | 통합 테스트 전면 부재 | P0 |
| QD-02 | DB↔ES 원자성 — Outbox 패턴 설계 | P0 |
| QD-03 | Pair → 명명된 VO 교체 | P2 |
| QD-04 | timestamp 신뢰 정책 + KST/UTC 경계 명세화 | P1 |
